## 一、什么是格式化分区

👉 **在分区上创建文件系统**
👉 会 **清空该分区上的所有数据**

常见文件系统：

- ext4（通用、稳定）
- xfs（大数据、高并发）
- swap（交换分区）

------

## 二、格式化前必做检查（非常重要）

```bash
lsblk -f
```

确认三点：

1. **分区名**（如 `/dev/sdb1`）
2. **是否未挂载**
3. **文件系统类型为空或确认可重建**

如果已挂载，先卸载：

```bash
umount /dev/sdb1
```

------

## 三、格式化常用命令（核心）

### 1️⃣ 格式化为 ext4（最常用）

```bash
mkfs.ext4 /dev/sdb1
```

带标签：

```bash
mkfs.ext4 -L data /dev/sdb1
```

------

### 2️⃣ 格式化为 xfs（数据盘推荐）

```bash
mkfs.xfs /dev/sdb1
```

⚠️ xfs 默认**不能覆盖已有文件系统**，如需强制：

```bash
mkfs.xfs -f /dev/sdb1
```

------

### 3️⃣ 格式化 swap 分区

```bash
mkswap /dev/sdb2
swapon /dev/sdb2
```

------

### 4️⃣ U 盘 / EFI 分区（fat32）

```bash
mkfs.vfat /dev/sdb1
```

------

## 四、格式化后挂载

### 1️⃣ 创建挂载点

```bash
mkdir -p /data
```

### 2️⃣ 临时挂载

```bash
mount /dev/sdb1 /data
```

验证：

```bash
df -Th
```

------

## 五、开机自动挂载（生产必做）

### 获取 UUID

```bash
blkid /dev/sdb1
```

### 编辑 `/etc/fstab`

```bash
UUID=xxxx-xxxx  /data  xfs  defaults  0 0
```

测试：

```bash
mount -a
```

------

## 六、格式化常见错误 & 排查

### ❌ 设备正被使用

```
umount: target is busy
```

排查：

```bash
lsof +D /data
fuser -m /data
```

------

### ❌ 覆盖系统盘（致命）

👉 操作前一定：

```bash
lsblk
```

------

## 七、实战场景示例（新数据盘）

```bash
lsblk
fdisk /dev/sdb        # 或 parted
mkfs.xfs /dev/sdb1
mkdir /data
mount /dev/sdb1 /data
blkid >> /etc/fstab
```

------

## 八、快速速查表

| 需求     | 命令                  |
| -------- | --------------------- |
| ext4     | `mkfs.ext4 /dev/sdb1` |
| xfs      | `mkfs.xfs /dev/sdb1`  |
| swap     | `mkswap /dev/sdb2`    |
| 查看类型 | `lsblk -f`            |
| 查看挂载 | `df -Th`              |

