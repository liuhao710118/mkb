`fdisk` 是 Linux 中**最经典、最常用的磁盘分区工具**，适合 **MBR 分区表、小于 2TB 的磁盘**，在很多系统初始化、救援场景中仍然必不可少。

------

## 一、fdisk 是做什么的

- 查看磁盘、分区信息
- 创建 / 删除 / 修改分区
- 管理 **MBR** 分区表
- 对小磁盘、老系统最稳定

📌 对比：

- **fdisk**：传统、稳
- parted：大盘、GPT
- gdisk：GPT 版 fdisk

------

## 二、查看磁盘信息

### 1️⃣ 查看所有磁盘

```bash
fdisk -l
```

示例：

```
Disk /dev/sda: 100 GiB
Disklabel type: dos
Device     Boot Start      End  Sectors Size Id Type
/dev/sda1  *     2048 209717247 209715200 100G 83 Linux
```

------

### 2️⃣ 查看指定磁盘

```bash
fdisk -l /dev/sda
```

------

## 三、交互式使用（最常用）

### 进入 fdisk

```bash
fdisk /dev/sdb
```

### 常用命令速查

| 命令 | 含义         |
| ---- | ------------ |
| `m`  | 帮助         |
| `p`  | 查看分区表   |
| `n`  | 新建分区     |
| `d`  | 删除分区     |
| `t`  | 修改分区类型 |
| `a`  | 设置启动分区 |
| `w`  | 写入并退出   |
| `q`  | 退出不保存   |

------

## 四、创建新分区（实战）

### 示例：新磁盘 `/dev/sdb`

```bash
fdisk /dev/sdb
n        # new
p        # primary
1        # 分区号
Enter    # 起始扇区
Enter    # 结束扇区（用满）
w        # 保存
```

格式化并挂载：

```bash
mkfs.ext4 /dev/sdb1
mount /dev/sdb1 /data
```

------

## 五、分区类型（t 命令）

```bash
t
L
```

常见类型：

| ID   | 类型       |
| ---- | ---------- |
| 83   | Linux      |
| 82   | Linux swap |
| 8e   | Linux LVM  |
| 7    | HPFS/NTFS  |

------

## 六、删除分区（危险操作）

```bash
fdisk /dev/sdb
d
1
w
```

⚠️ **只删除分区表，不等于立即清数据，但非常危险**

------

## 七、fdisk 扩容说明（重要）

❗ fdisk **不能在线扩容分区**
❗ 通常流程：

1. 删除分区（不删除数据）
2. 重建同起始扇区的新分区
3. 扩展文件系统

📌 **生产环境慎用，建议用 parted**

------

## 八、fdisk vs parted vs gdisk

| 工具   | MBR  | GPT  | >2TB |
| ------ | ---- | ---- | ---- |
| fdisk  | ✅    | ⚠️    | ❌    |
| parted | ✅    | ✅    | ✅    |
| gdisk  | ❌    | ✅    | ✅    |

------

## 九、常见运维场景

### 🔹 查看系统盘分区

```bash
fdisk -l /dev/sda
```

### 🔹 创建 swap 分区

```bash
fdisk /dev/sdb
t → 82
```

------

## 十、速查总结

```bash
fdisk -l
fdisk /dev/sdb
n p 1
w
```

