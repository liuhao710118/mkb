# 👤 **usermod 命令详解**

`usermod` 用于 **修改已存在用户的属性**，只能由 **root** 使用。

------

# 🧩 **基本语法**

```bash
usermod [选项] 用户名
```

------

# 📌 **常用选项**

| 选项          | 说明                                    |
| ------------- | --------------------------------------- |
| `-c "备注"`   | 修改用户备注（comment）                 |
| `-d 目录`     | 修改用户家目录                          |
| `-m`          | 配合 `-d`，将旧家目录内容移动到新家目录 |
| `-g 组名`     | 修改用户的主组                          |
| `-G 组1,组2`  | 设置用户附加组（覆盖原有附加组）        |
| `-aG`         | 向现有附加组 **追加** 组（不会覆盖）    |
| `-s shell`    | 修改用户登录 shell                      |
| `-u UID`      | 修改用户 UID                            |
| `-l 新用户名` | 修改登录名（重命名用户）                |
| `-L`          | 锁定用户（Lock）                        |
| `-U`          | 解锁用户（Unlock）                      |
| `-e 日期`     | 设置账号过期日期（YYYY-MM-DD）          |
| `-f 天数`     | 密码过期后多少天禁用账号                |

------

# 🧪 **常用操作示例**

------

### ⭐ **1. 修改用户备注**

```bash
usermod -c "Web Server Admin" nginx
```

------

### ⭐ **2. 修改用户家目录**

```bash
usermod -d /home/newuser -m olduser
```

`-m` 表示自动将旧目录文件迁移到新目录。

------

### ⭐ **3. 修改主组**

```bash
usermod -g developers user1
```

------

### ⭐ **4. 添加用户到附加组**

> **推荐用法：不会覆盖已有附加组**

```bash
usermod -aG wheel user1
```

添加到 `wheel`（sudo）组。

------

### ⭐ **5. 设置登录 shell**

```bash
usermod -s /bin/bash user1
```

------

### ⭐ **6. 修改用户名（重命名用户）**

```bash
usermod -l newname oldname
```

------

### ⭐ **7. 锁定用户**

```bash
usermod -L user1
```

效果同 `passwd -l`（在 shadow 密码前加 `!`）

------

### ⭐ **8. 解锁用户**

```bash
usermod -U user1
```

------

### ⭐ **9. 设置账号过期时间**

```bash
usermod -e 2025-12-31 user1
```

这日期后账号无法登录。

------

### ⭐ **10. 设置密码过期后宽限天数**

```bash
usermod -f 7 user1
```

表示密码过期后 7 天再禁用账号。

------

# 🔎 **查看修改结果**

```bash
id user1
```

查看 UID/GID/组信息。

```bash
cat /etc/passwd
cat /etc/group
```

