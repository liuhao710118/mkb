# 📘 Linux `cat 命令教程文档

## 一、命令简介

`cat`（concatenate）命令用于：

1. **查看文件内容**
2. **创建文件**
3. **将多个文件合并输出**
4. **输出内容到终端或重定向到文件**

是 Linux 中最常用的文本处理工具之一。

------

## 二、命令格式

```bash
cat [选项] [文件...]
```

------

## 三、常用选项说明

| 选项 | 作用                              |
| ---- | --------------------------------- |
| `-n` | 显示行号（所有行）                |
| `-b` | 显示行号（非空行）                |
| `-E` | 显示行尾符 `$`                    |
| `-s` | 压缩连续的空行为一行              |
| `-T` | 将 TAB 显示为 `^I`                |
| `-A` | 显示所有不可见字符（等于 `-vET`） |
| `-v` | 显示不可打印字符（Tab、换行除外） |

------

## 四、基础用法示例

### 1. 查看文件内容

```bash
cat file.txt
```

------

### 2. 查看多个文件内容

```bash
cat file1.txt file2.txt
```

------

### 3. 合并文件写入新文件

```bash
cat file1 file2 > merged.txt
```

👉 `merged.txt` 不存在时自动创建。

------

### 4. 在文件末尾追加内容

```bash
cat file1 >> file2
```

------

### 5. 将字符串写入新文件

利用重定向方式：

```bash
echo "hello world" > test.txt
```

也可以用 cat 创建文件：

```bash
cat > notes.txt
第一行内容
第二行内容
按 Ctrl + D 保存退出
```

------

### 6. 追加文本到文件末尾

```bash
cat >> notes.txt
追加内容
再次按 Ctrl + D 保存
```

------

## 五、常用显示格式示例

### 1. 显示行号

```bash
cat -n file.txt
```

------

### 2. 仅为非空行显示行号

```bash
cat -b file.txt
```

------

### 3. 显示行尾符（调试文本格式）

```bash
cat -E file.txt
```

------

### 4. 显示 TAB 字符

```bash
cat -T file.txt
```

------

### 5. 显示不可见字符（调试编码问题）

```bash
cat -A file.txt
```

------

### 6. 去除多余空行

```bash
cat -s file.txt
```

------

## 六、输出重定向与技巧

### 1. 内容覆盖写入文件

```bash
cat > file.txt
```

### 2. 内容追加写入文件

```bash
cat >> file.txt
```

### 3. 合并多个文件

```bash
cat file1 file2 file3 > output.txt
```

### 4. 合并多个配置片段

```bash
cat part1.conf part2.conf > final.conf
```

------

## 七、常见场景示例

### 场景 1：查看日志（不分页）

```bash
cat /var/log/syslog
```

（建议用 `less` 或 `tail -f` 查看大文件）

------

### 场景 2：调试文本编码或格式

```bash
cat -A file.txt
```

------

### 场景 3：快速创建文本文件

```bash
cat > todo.txt
- item1
- item2
```

------

### 场景 4：复制文件（简单替代 cp）

```bash
cat source.txt > copy.txt
```

------

### 场景 5：用 Here Document 生成内容

```bash
cat <<EOF > config.txt
server=127.0.0.1
port=8080
EOF
```

------

## 八、与其他命令组合使用（重点）

### 1. 与 grep 组合（查找关键字）

```bash
cat file.txt | grep "error"
```

------

### 2. 与 sort 组合（排序）

```bash
cat names.txt | sort
```

------

### 3. 与 wc 组合（统计行数/字数/字符）

```bash
cat file.txt | wc -l
```

------

### 4. 与 head / tail 组合

```bash
cat file.txt | head -5
cat file.txt | tail -10
```

------

## 九、常见错误及解决方法

| 错误                                   | 原因       | 解决方法               |
| -------------------------------------- | ---------- | ---------------------- |
| `cat: file: No such file or directory` | 文件不存在 | 检查路径               |
| `cat: file: Permission denied`         | 权限不足   | 使用 `sudo` 或修改权限 |
| 输出乱码                               | 编码不一致 | 使用 `iconv` 转编码    |

------

## 十、命令总结表

| 操作           | 示例                        |
| -------------- | --------------------------- |
| 查看文件内容   | `cat file.txt`              |
| 合并文件       | `cat file1 file2 > out.txt` |
| 新建文件       | `cat > new.txt`             |
| 追加内容       | `cat >> file.txt`           |
| 显示行号       | `cat -n file.txt`           |
| 显示不可见字符 | `cat -A file.txt`           |
| 显示 TAB       | `cat -T file.txt`           |
| 去除多余空行   | `cat -s file.txt`           |

------

