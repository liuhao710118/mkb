
# 🧩 Linux `zip` / `unzip` 命令使用教程

------

## 一、命令简介

`zip` 和 `unzip` 是 Linux 中最常用的**压缩与解压工具**之一，兼容 Windows 的 `.zip` 格式。

特点：

- 支持跨平台（Linux、Windows、macOS）；
- 可递归压缩整个目录；
- 支持密码保护与排除文件；
- 可选择压缩级别；
- 速度较快、兼容性强。

------

## 二、基本语法

### 📦 压缩命令（zip）

```bash
zip [选项] 压缩包名.zip 文件或目录...
```

### 📂 解压命令（unzip）

```bash
unzip [选项] 压缩包名.zip
```

------

## 三、常用选项说明

### 🗜️ zip 常用选项

| 选项 | 含义                         |
| ---- | ---------------------------- |
| `-r` | 递归压缩目录及子目录         |
| `-9` | 最大压缩率（`-1` ~ `-9`）    |
| `-q` | 静默模式，不显示过程         |
| `-v` | 显示详细信息                 |
| `-e` | 使用密码加密压缩包           |
| `-x` | 排除匹配的文件或目录         |
| `-m` | 压缩后删除源文件（移动模式） |
| `-T` | 检查压缩包完整性             |

------

### 📂 unzip 常用选项

| 选项 | 含义                   |
| ---- | ---------------------- |
| `-d` | 指定解压目标目录       |
| `-l` | 列出压缩包内容，不解压 |
| `-o` | 覆盖已有文件而不询问   |
| `-v` | 显示详细信息           |
| `-n` | 不覆盖已有文件         |
| `-q` | 静默模式               |
| `-P` | 指定解压密码           |

------

## 四、常见操作示例

### 1️⃣ 压缩单个文件

```bash
zip archive.zip file.txt
```

> 将 `file.txt` 压缩为 `archive.zip`

------

### 2️⃣ 压缩多个文件

```bash
zip archive.zip file1.txt file2.txt image.png
```

------

### 3️⃣ 压缩整个目录（递归）

```bash
zip -r backup.zip /home/user/docs
```

> 将 `/home/user/docs` 目录及子目录全部打包。

------

### 4️⃣ 指定压缩级别

```bash
zip -r -9 backup.zip /home/user/docs
```

> 使用最高压缩率。

------

### 5️⃣ 压缩并加密

```bash
zip -re secret.zip /home/user/private
```

> 系统将提示你输入密码；解压时需要输入相同密码。

------

### 6️⃣ 压缩时排除文件

```bash
zip -r backup.zip /home/user --exclude '*.log' --exclude 'tmp/*'
```

> 排除所有 `.log` 文件和 `tmp` 目录。

------

### 7️⃣ 解压到当前目录

```bash
unzip archive.zip
```

------

### 8️⃣ 解压到指定目录

```bash
unzip archive.zip -d /opt/restore
```

------

### 9️⃣ 解压加密文件

```bash
unzip -P 密码 secret.zip
```

> 若不指定 `-P`，会提示输入密码。

------

### 🔟 不覆盖已存在文件

```bash
unzip -n archive.zip
```

> 保留原文件，只解压不存在的文件。

------

## 五、查看与测试

| 功能                | 命令                   |
| ------------------- | ---------------------- |
| 查看压缩包内容      | `unzip -l archive.zip` |
| 查看详细文件信息    | `unzip -v archive.zip` |
| 检查压缩包完整性    | `unzip -t archive.zip` |
| 检查 zip 包是否损坏 | `zip -T archive.zip`   |

------

## 六、分卷压缩与合并


### 🧩 一、分卷压缩的概念

当一个压缩包文件很大（例如几个 GB），可以使用分卷压缩将它拆成多段小文件，例如：

```
backup.zip.001
backup.zip.002
backup.zip.003
...
```

这些小文件每个大小固定（比如 100MB），合在一起才能还原完整压缩包。

---

### 🧠 二、分卷压缩的主要好处

| 优点                  | 说明                                           |
| ------------------- | -------------------------------------------- |
| 🧱 **方便传输**         | 大文件可能受限于网络、邮件或存储系统（如只能上传 100MB 以下），分卷后可逐个传输。 |
| 💾 **便于存储与备份**      | 可以分散在不同介质（U 盘、光盘、磁带）上保存，降低单点损坏风险。            |
| 🔁 **出错可单独重传**      | 如果某个分卷损坏或传输失败，只需重新上传该分卷，而不用重传整个压缩包。          |
| 🪶 **兼容旧系统或文件系统限制** | 某些文件系统（如 FAT32）不支持超过 4GB 的单个文件，分卷可以避免此限制。    |
| 🧰 **易于分发**         | 在软件发布、备份归档、离线传输（如通过多张光盘）时更方便。                |
| 🔒 **可结合加密与校验**     | 每个分卷都可单独加密或校验完整性，提升数据安全性。                    |

---

### 1️⃣ 使用 zip 进行分卷压缩

#### （1）压缩命令

```bash
zip -r -s 100m backup.zip /path/to/data
```

**参数说明：**

* `-r`：递归压缩目录；
* `-s 100m`：每卷大小 100MB，可改为 500m、1g 等；
* `backup.zip`：主文件名；
* `/path/to/data`：要压缩的文件或目录。

**执行结果示例：**

```
backup.z01
backup.z02
backup.z03
backup.zip
```

> 💡 `.zip` 是最后一卷文件。所有分卷必须保存在同一目录下。

---

#### （2）合并分卷（可选）

当你要进行解压时，`unzip` 可直接识别分卷，也可先合并成完整包：

```bash
zip -s 0 backup.zip --out merged_backup.zip
```

> 该命令会将所有分卷重新组合成 `merged_backup.zip`，方便单次解压。

---

#### （3）解压命令

直接解压分卷压缩包：

```bash
unzip backup.zip -d /opt/restore
```

**说明：**

* `unzip` 会自动识别并解压所有相关分卷（`.z01`、`.z02`...）。
* `-d` 指定解压路径。

**解压输出示例：**

```
Archive:  backup.zip
 extracting: /opt/restore/file1.txt
 extracting: /opt/restore/file2.txt
 extracting: /opt/restore/subdir/file3.log
```

---

### 2️⃣ 使用 tar + split 实现分卷压缩

#### （1）压缩命令

```bash
tar -czf - /path/to/data | split -b 100M - backup.tar.gz.part-
```

**参数说明：**

* `tar -czf -`：将压缩结果输出到标准输出；
* `split -b 100M - backup.tar.gz.part-`：按 100MB 分割输出；
* 生成文件示例：

  ```
  backup.tar.gz.part-aa
  backup.tar.gz.part-ab
  backup.tar.gz.part-ac
  ...
  ```

---

#### （2）合并并解压命令

```bash
cat backup.tar.gz.part-* | tar -xzvf -
```

**说明：**

* `cat` 将所有分卷按顺序拼接；
* 管道传给 `tar` 解压；
* `-x`：解压，`-z`：gzip，`-v`：显示过程，`-f -`：从标准输入读取。

---

### 3️⃣ 验证压缩包完整性

#### 对于 zip 分卷包：

```bash
unzip -t backup.zip
```

#### 对于 tar 分卷包：

```bash
cat backup.tar.gz.part-* | tar -tzf - > /dev/null
```

> 若命令执行正常且无错误输出，说明压缩包完整无损。

---

### ⚠️ 四、注意事项

1. 分卷文件必须 **全部在同一目录下** 才能成功合并。
2. 解压时**不能直接解压分卷之一**，必须合并后再解压。
3. 命名顺序要保持正确，否则会导致合并失败。
4. 分卷压缩适合**静态归档数据**（例如备份），不适合频繁更新的文件。

---

### 🧰 五、适用场景举例

| 场景                    | 原因            |
| --------------------- | ------------- |
| 通过邮件发送大文件             | 邮件系统通常限制附件大小  |
| 上传到云盘或 GitHub Release | 平台限制单文件大小     |
| 离线分发软件安装包             | 可拆分成多张 U 盘或光盘 |
| 跨系统备份                 | 避免文件系统大小限制    |
| 网络传输不稳定               | 分卷便于断点续传或重试   |

---

✅ **一句总结：**

> 分卷压缩的本质是 “以便传输、容错与兼容”，
> 它牺牲一点操作复杂度，换取了**可控性与可靠性**。

------

## 七、压缩比与性能

| 压缩参数 | 说明                 | 适用场景 |
| -------- | -------------------- | -------- |
| `-1`     | 最快压缩，压缩率低   | 临时打包 |
| `-6`     | 默认压缩率           | 通用     |
| `-9`     | 最慢压缩，压缩率最高 | 归档存储 |

------

## 八、实用技巧总结

| 需求                     | 命令                                                   |
| ------------------------ | ------------------------------------------------------ |
| 压缩当前目录所有内容     | `zip -r all.zip ./*`                                   |
| 压缩并删除源文件         | `zip -m archive.zip file1 file2`                       |
| 查看压缩包文件大小与时间 | `unzip -l archive.zip`                                 |
| 批量解压多个 ZIP 文件    | `for f in *.zip; do unzip -o "$f" -d extracted/; done` |
| 在脚本中静默解压         | `unzip -oq archive.zip -d /tmp/data`                   |

------

## 九、注意事项

1. **文件名含空格时**需使用引号：

   ```bash
   zip "my archive.zip" "My Folder/*"
   ```

2. **Linux 与 Windows 编码差异**：
    使用 `unzip -O UTF-8 file.zip` 可修复乱码。

3. **权限问题**：
    `zip` 默认不会保留 Linux 权限；若需保留建议用 `tar`。

4. **大文件压缩限制**：
    `.zip` 对单文件大小的支持取决于版本，建议使用 `zip >= 3.0`。

------

## 十、常用命令速查表

| 功能           | 命令                                       |
| -------------- | ------------------------------------------ |
| 压缩文件或目录 | `zip -r archive.zip dir`                   |
| 加密压缩       | `zip -re archive.zip dir`                  |
| 排除文件       | `zip -r archive.zip dir --exclude '*.log'` |
| 解压文件       | `unzip archive.zip`                        |
| 解压到指定目录 | `unzip archive.zip -d /path`               |
| 查看内容       | `unzip -l archive.zip`                     |
| 测试压缩包     | `unzip -t archive.zip`                     |



