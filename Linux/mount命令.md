`mount` 是 Linux 中**文件系统挂载的核心命令**，用于把一个分区 / 设备 / 网络存储 **挂到某个目录上使用**，属于运维必会命令之一。

------

## 一、mount 是干什么的

👉 建立 **设备 ↔ 目录** 的映射关系
👉 挂载后才能访问磁盘内容
👉 一切文件访问都从 `/` 开始

示意（逻辑）：

```
/dev/sdb1  →  /data
```

------

## 二、最常用的基本用法

### 1️⃣ 挂载分区（最常见）

```bash
mount /dev/sdb1 /data
```

前提：

- `/data` 目录存在
- 分区已格式化
- 未被挂载

------

### 2️⃣ 自动识别文件系统

```bash
mount /dev/sdb1 /data
```

Linux 会自动识别 ext4 / xfs / vfat 等

------

### 3️⃣ 指定文件系统类型

```bash
mount -t xfs /dev/sdb1 /data
```

------

## 三、查看挂载信息

### 查看所有挂载

```bash
mount
```

### 推荐（清晰）

```bash
df -Th
lsblk
```

------

## 四、常用参数（运维高频）

| 参数 | 作用              |
| ---- | ----------------- |
| `-t` | 指定文件系统类型  |
| `-o` | 挂载选项          |
| `-a` | 挂载 fstab 中所有 |
| `-r` | 只读挂载          |
| `-w` | 读写挂载          |

------

## 五、挂载选项（-o）

### 常见选项

```bash
mount -o rw,noatime /dev/sdb1 /data
```

| 选项     | 含义           |
| -------- | -------------- |
| rw       | 读写           |
| ro       | 只读           |
| noatime  | 不更新访问时间 |
| defaults | 默认           |

------

## 六、临时 vs 永久挂载（重点）

### 临时挂载（重启失效）

```bash
mount /dev/sdb1 /data
```

------

### 永久挂载（生产必做）

#### 1️⃣ 获取 UUID

```bash
blkid /dev/sdb1
```

#### 2️⃣ 编辑 `/etc/fstab`

```bash
UUID=xxxx-xxxx  /data  xfs  defaults  0 0
```

#### 3️⃣ 验证

```bash
mount -a
```

------

## 七、特殊挂载场景

### 🔹 挂载 ISO 文件

```bash
mount -o loop CentOS.iso /mnt
```

------

### 🔹 挂载 NFS

```bash
mount -t nfs 10.0.0.10:/share /mnt
```

------

### 🔹 挂载 tmpfs（内存）

```bash
mount -t tmpfs tmpfs /mnt
```

------

## 八、卸载（umount）

```bash
umount /data
```

如果提示 busy：

```bash
lsof +D /data
fuser -m /data
```

强制（慎用）：

```bash
umount -l /data
```

------

## 九、常见错误 & 排查

### ❌ unknown filesystem

👉 未格式化

```bash
mkfs.ext4 /dev/sdb1
```

### ❌ already mounted

👉 已挂载

```bash
mount | grep sdb1
```

------

## 十、运维速查

```bash
mount /dev/sdb1 /data
df -Th
mount -a
umount /data
```

