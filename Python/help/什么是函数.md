# 🐍 Python 函数详解（超清晰）

## 1. 什么是函数？

函数（function）是 **组织好的、可重复使用的代码块**，用于实现某一功能。

```python
def say_hello():
    print("Hello!")
```

调用：

```python
say_hello()
```

------

# 2. 函数的定义语法

```python
def 函数名(参数列表):
    """可选的文档字符串"""
    函数体
    return 返回值
```

示例：

```python
def add(a, b):
    return a + b
```

------

# 3. 参数类型详解（Python 最容易迷糊的地方）

## 3.1 必需参数（Positional Arguments）

必须传的参数。

```python
def mul(a, b):
    return a * b

mul(2, 3)
```

------

## 3.2 默认参数（Default Arguments）

给参数默认值，可以不传。

```python
def greet(name="Guest"):
    print("Hello", name)

greet()
greet("Jack")
```

⚠ **危险点：默认值只在函数定义时创建一次**
尤其是可变对象（list、dict）

```python
def foo(a=[], b={}):   # 不推荐
    ...
```

------

## 3.3 可变参数（*args）

接收任意数量的位置参数。

```python
def sum_all(*nums):
    return sum(nums)

sum_all(1,2,3,4)
```

`args` 是一个 **tuple**。

------

## 3.4 关键字参数（**kwargs）

接收任意数量的“键=值”参数。

```python
def show_info(**infos):
    print(infos)

show_info(name="Liu", age=18)
```

`kwargs` 是一个 **dict**。

------

## 3.5 参数顺序（必须记住）

顺序必须是：

```
必需参数 → 默认参数 → *args → **kwargs
```

示例（正确）：

```python
def func(a, b=0, *args, **kwargs):
    pass
```

------

# 4. return 返回值

## 4.1 返回单个值

```python
def f():
    return 10
```

## 4.2 返回多个值（Python 自动打包为 tuple）

```python
def calc(a, b):
    return a+b, a*b

s, m = calc(2, 5)
```

------

# 5. 文档字符串（docstring）

用于说明函数功能，配合 `help()` 使用。

```python
def add(a, b):
    """Return the sum of a and b."""
    return a + b

help(add)
```

------

# 6. 匿名函数（lambda）

```python
add = lambda x, y: x + y
```

常用于排序、多值映射等简短场景：

```python
sorted(data, key=lambda x: x.age)
```

------

# 7. 函数是“第一类对象”（重要概念）

函数可以：

- 赋值给变量
- 放到 list、dict 中
- 作为参数传递
- 作为返回值

```python
def hi():
    print("Hi")

f = hi
f()
```

------

# 8. 闭包（closure）——高阶函数核心概念

函数内部定义函数，并返回内部函数。

```python
def outer(x):
    def inner(y):
        return x + y
    return inner

f = outer(10)
f(5)   # 15
```

------

# 9. 装饰器（decorator）——Python 进阶最重要概念之一

本质：接受一个函数作为输入，返回一个新的函数。

```python
def log(func):
    def wrapper(*args, **kwargs):
        print("call", func.__name__)
        return func(*args, **kwargs)
    return wrapper

@log
def hello():
    print("Hello")

hello()
```

------

# 10. 类型注解（type hints）

增强可读性 + IDE 提示，不影响运行。

```python
def add(a: int, b: int) -> int:
    return a + b
```

------

# 11. Python 函数的作用域（LEGB）

查找变量顺序：

```
L：Local（局部）
E：Enclosing（外层函数）
G：Global（全局）
B：Built-in（内建）
```

示例：

```python
x = 1
def outer():
    x = 2
    def inner():
        x = 3
        return x
    return inner()
```

------

# 12. 内置函数（一些最常用的）

| 函数           | 说明       |
| -------------- | ---------- |
| `len()`        | 长度       |
| `range()`      | 范围生成器 |
| `map()`        | 映射函数   |
| `filter()`     | 过滤       |
| `sorted()`     | 排序       |
| `sum()`        | 求和       |
| `isinstance()` | 类型检查   |
| `enumerate()`  | 带索引遍历 |

------

# 13. 何时使用函数？

- 逻辑重复
- 逻辑过长需要拆分
- 提高可读性
- 需要抽象行为

